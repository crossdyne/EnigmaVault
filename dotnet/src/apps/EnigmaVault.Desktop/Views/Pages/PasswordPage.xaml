<Page x:Class="EnigmaVault.Desktop.Views.Pages.PasswordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EnigmaVault.Desktop.Views.Pages"
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:behaviors="clr-namespace:EnigmaVault.Desktop.Resources.Behaviors"
      xmlns:sys="clr-namespace:System;assembly=System.Runtime"
      xmlns:attach="clr-namespace:EnigmaVault.Desktop.Resources.Attachable"
      xmlns:enum="clr-namespace:EnigmaVault.Desktop.Enums" 
      xmlns:converters="clr-namespace:EnigmaVault.Desktop.Resources.ValueConverters" 
      xmlns:selectors="clr-namespace:EnigmaVault.Desktop.Selectors"
      xmlns:pages="clr-namespace:EnigmaVault.Desktop.ViewModels.Pages" 
      xmlns:uc="clr-namespace:EnigmaVault.Desktop.Views.UserControls" 
      xmlns:cc="clr-namespace:EnigmaVault.Desktop.Views.Controls" 
      xmlns:pvm="clr-namespace:EnigmaVault.Desktop.ViewModels.Features.Credentials.Items"
      d:DataContext="{d:DesignInstance Type=pages:PasswordPageViewModel}"
      mc:Ignorable="d" 
      Loaded="PasswordPage_Loaded" 
      Unloaded="PasswordPage_Unloaded"
      Title="PasswordPage"
      Background="Transparent"
      x:Name="PassPage">
    
    <b:Interaction.Behaviors>
        <behaviors:AsyncInitializeBehavior />
    </b:Interaction.Behaviors>

    <Page.Resources>
        <converters:PasswordPageControlsVisibilityConverter x:Key="PasswordPageControlsVisibilityConverter"/>
        <converters:ActionOnDataToVisibilityConverter x:Key="ActionOnDataToVisibilityConverter"/>
        <converters:ActionOnDataToIsReadOnlyConverter x:Key="ActionOnDataToIsReadOnlyConverter"/>
        <converters:ActionOnDataToPreviewIconVisibilityConverter x:Key="ActionOnDataToPreviewIconVisibilityConverter"/>
        <converters:ActionOnDataToCopyVisibilityConverter x:Key="ActionOnDataToCopyVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:DateToDaysRemainingConverter x:Key="DateToDaysRemainingConverter"/>

        <!--Разметка паролей-->

        <!--#region Password -->

        <DataTemplate DataType="{x:Type pvm:StandardPasswordViewModel}">
            <DockPanel>
                <DockPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DockPanel.Resources>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Основная информация" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <Image Source="{Binding Icon}"
                       Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToPreviewIconVisibilityConverter}}"
                       Height="100"
                       DockPanel.Dock="Top"/>

                <DockPanel DockPanel.Dock="Top">

                    <Button Style="{StaticResource Button.App}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToCopyVisibilityConverter}}"
                            DockPanel.Dock="Right">
                        <Button.ToolTip>
                            <ToolTip Content="Копировать" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding DataContext.TopToolTipController.CustomPlacementCallback, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <Button.Content>
                            <Control Template="{StaticResource Copy}" Width="15"/>
                        </Button.Content>
                    </Button>

                    <cc:PlaceHolderTextBox PlaceHolder="Название" 
                                           Text="{Binding ServiceName}"
                                           IsReadOnly="{Binding IsReadOnly}" 
                                           HorizontalAlignment="Stretch"/>

                </DockPanel>

                <cc:PlaceHolderTextBox PlaceHolder="Url" 
                                       Text="{Binding Url}" 
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Инф. для шифрования" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <cc:PlaceHolderTextBox PlaceHolder="Логин" 
                                       Text="{Binding Login}" 
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Пароль"
                                       Text="{Binding Password}" 
                                       IsReadOnly="{Binding IsReadOnly}"
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Почта"
                                       Text="{Binding Email}" 
                                       IsReadOnly="{Binding IsReadOnly}"
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Телефон" 
                                       Text="{Binding Phone}"
                                       IsReadOnly="{Binding IsReadOnly}"
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Секретная фраза" 
                                       Text="{Binding SecretWord}" 
                                       IsReadOnly="{Binding IsReadOnly}"
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Ключи восстановления"
                                       Text="{Binding RecoveryKey}" 
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Заметки (Цифруются)" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <Grid DockPanel.Dock="Bottom">
                    <Button Content="Сохранить"
                            Command="{Binding DataContext.CreateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Create}}"
                            Style="{StaticResource Button.Save}" 
                            VerticalAlignment="Bottom"/>

                    <Button Content="Обновить"
                            Command="{Binding DataContext.UpdateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Update}}"
                            Style="{StaticResource Button.Update}" 
                            VerticalAlignment="Bottom"/>
                </Grid>

                <TextBox Text="{Binding Note}" 
                         IsReadOnly="{Binding IsReadOnly}"
                         VerticalAlignment="Stretch"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>

            </DockPanel>
        </DataTemplate>

        <!--#endregion-->

        <!--#region Server -->

        <DataTemplate DataType="{x:Type pvm:ServerPasswordViewModel}">
            <DockPanel>
                <DockPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DockPanel.Resources>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Основная информация" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <Image Source="{Binding Icon}"
                       Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToPreviewIconVisibilityConverter}}"
                       Height="100"
                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Название" 
                                       Text="{Binding ServiceName}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Url" 
                                       Text="{Binding Url}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>


                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Инф. для шифрования" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <cc:PlaceHolderTextBox PlaceHolder="IP Адрес" 
                                       Text="{Binding IpAddress}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="SSH Порт" 
                                       Text="{Binding Port}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Домен" 
                                       Text="{Binding Domain}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Логин" 
                                       Text="{Binding Login}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Root пароль" 
                                       Text="{Binding RootPassword}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="SSH Ключ" 
                                       Text="{Binding SshKey}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Grid DockPanel.Dock="Bottom">
                    <Button Content="Сохранить"
                            Command="{Binding DataContext.CreateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Create}}"
                            Style="{StaticResource Button.Save}" 
                            Margin="0,0,5,0"
                            VerticalAlignment="Bottom"/>

                    <Button Content="Обновить"
                            Command="{Binding DataContext.UpdateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Update}}"
                            Style="{StaticResource Button.Update}" 
                            Margin="0,0,5,0"
                            VerticalAlignment="Bottom"/>
                </Grid>

                <TextBlock Text="Заметка" DockPanel.Dock="Top"/>

                <TextBox Text="{Binding Note}" 
                         IsReadOnly="{Binding IsReadOnly}"
                         VerticalAlignment="Stretch"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>

            </DockPanel>
        </DataTemplate>

        <!--#endregion-->

        <!--#region ApiKey -->

        <DataTemplate DataType="{x:Type pvm:ApiKeyViewModel}">
            <DockPanel>
                <DockPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DockPanel.Resources>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Основная информация" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <Image Source="{Binding Icon}"
                       Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToPreviewIconVisibilityConverter}}"
                       Height="100"
                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Название" 
                                       Text="{Binding ServiceName}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Url" 
                                       Text="{Binding Url}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Инф. для шифрования" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <cc:PlaceHolderTextBox PlaceHolder="Ключ" 
                                       Text="{Binding ApiKey}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Сайт" 
                                       Text="{Binding BaseUrl}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>
                                       
                <cc:PlaceHolderTextBox PlaceHolder="Идентификатор клиента" 
                                       Text="{Binding ClientId}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>
                                       
                <cc:PlaceHolderTextBox PlaceHolder="Секрет клиента" 
                                       Text="{Binding ClientSecret}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Дата окончания" 
                                       Text="{Binding ExpirationDate}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Окружение" 
                                       Text="{Binding Environment}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Область" 
                                       Text="{Binding Scope}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Grid DockPanel.Dock="Bottom">
                    <Button Content="Сохранить"
                            Command="{Binding DataContext.CreateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Create}}"
                            Style="{StaticResource Button.Save}" 
                            Margin="0,0,5,0"
                            VerticalAlignment="Bottom"/>

                    <Button Content="Обновить"
                            Command="{Binding DataContext.UpdateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Update}}"
                            Style="{StaticResource Button.Update}" 
                            Margin="0,0,5,0"
                            VerticalAlignment="Bottom"/>
                </Grid>

                <TextBlock Text="Заметка" DockPanel.Dock="Top"/>

                <TextBox Text="{Binding Note}" 
                         IsReadOnly="{Binding IsReadOnly}"
                         VerticalAlignment="Stretch"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>

            </DockPanel>
        </DataTemplate>

        <!--#endregion-->

        <!--#region Банковские карты -->

        <DataTemplate DataType="{x:Type pvm:CreditCardViewModel}">
            <DockPanel>
                <DockPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DockPanel.Resources>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Основная информация" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <Image Source="{Binding Icon}"
                       Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToPreviewIconVisibilityConverter}}"
                       Height="100"
                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Название" 
                                       Text="{Binding ServiceName}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Url" 
                                       Text="{Binding Url}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Border Background="#3B3F42" Margin="0,5,0,5" CornerRadius="6" DockPanel.Dock="Top">
                    <TextBlock Text="Инф. для шифрования" HorizontalAlignment="Center" Margin="0,5,0,5"/>
                </Border>

                <cc:PlaceHolderTextBox PlaceHolder="Номер карты" 
                                       Text="{Binding CardNumber}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Владелец карты" 
                                       Text="{Binding CardHolder}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Дата окончания" 
                                       Text="{Binding ExpiryDate}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="CVV" 
                                       Text="{Binding CvvCode}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Pin-code" 
                                       Text="{Binding PinCode}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Название банка" 
                                        Text="{Binding BankName}"
                                        IsReadOnly="{Binding IsReadOnly}" 
                                        DockPanel.Dock="Top"/>

                <cc:PlaceHolderTextBox PlaceHolder="Платежная система" 
                                       Text="{Binding PaymentSystem}"
                                       IsReadOnly="{Binding IsReadOnly}" 
                                       DockPanel.Dock="Top"/>

                <Grid DockPanel.Dock="Bottom">
                    <Button Content="Сохранить"
                            Command="{Binding DataContext.CreateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Create}}"
                            Style="{StaticResource Button.Save}" 
                            Margin="0,0,5,0"
                            VerticalAlignment="Bottom"/>

                    <Button Content="Обновить"
                            Command="{Binding DataContext.UpdateVaultCommand, RelativeSource={RelativeSource AncestorType=ContentControl}}"
                            Visibility="{Binding DataContext.CurrentActionRightSideMenu, RelativeSource={RelativeSource AncestorType=ContentControl}, Converter={StaticResource ActionOnDataToVisibilityConverter}, ConverterParameter={x:Static enum:ActionOnData.Update}}"
                            Style="{StaticResource Button.Update}" 
                            Margin="0,0,5,0"
                            VerticalAlignment="Bottom"/>
                </Grid>

                <TextBlock Text="Заметка" DockPanel.Dock="Top"/>

                <TextBox Text="{Binding Note}" 
                         IsReadOnly="{Binding IsReadOnly}"
                         VerticalAlignment="Stretch"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>

            </DockPanel>
        </DataTemplate>

        <!--#endregion-->

    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="250" MaxWidth="700"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" d:Width="250"/>
        </Grid.ColumnDefinitions>

        <!--==========Popups==========-->

        <!--#region История изменений записи -->


        <!--#endregion-->

        <!--#region Архивные записи -->

        <Popup IsOpen="{Binding ArchivesPopup.IsOpen, Mode=TwoWay}"
               PlacementTarget="{Binding ElementName=OpenArchiveManagementButton}" 
               Placement="Custom"
               CustomPopupPlacementCallback="{Binding ArchivesPopup.CustomPlacementCallback}"
               StaysOpen="False"
               PopupAnimation="Fade"
               AllowsTransparency="True"
               Width="300"
               MaxHeight="{Binding ArchivesPopup.MaxHeight}">
            <Border Background="#282828"
                    BorderBrush="White"
                    BorderThickness="0.5"
                    Padding="0" 
                    CornerRadius="5">
                <ListView ItemsSource="{Binding ArchivedPasswords}" 
                          SelectedItem="{Binding SelectedArchivedEncryptedOverview}"
                          SelectionMode="Single"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalContentAlignment="Stretch"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.CanContentScroll="False">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch" Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" 
                                       Source="{Binding Icon}" 
                                       Width="27" 
                                       Height="27" 
                                       Margin="5,0,10,0"
                                       VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1" 
                                           Text="{Binding ServiceName}" 
                                           Foreground="White" 
                                           FontSize="18"
                                           VerticalAlignment="Center" 
                                           TextTrimming="CharacterEllipsis"/>

                                <Button Grid.Column="2"
                                        Style="{StaticResource Button.ListView.Menu}"
                                        VerticalAlignment="Center"
                                        Margin="5,0">
                                    <Button.Content>
                                        <Control Template="{StaticResource ArchiveRestore}" Width="20" Foreground="White"/>
                                    </Button.Content>
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                                    PropertyName="IsSelected" Value="True" />
                                            <b:InvokeCommandAction Command="{Binding DataContext.SetArchiveCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                                   CommandParameter="{Binding}"/>
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource ListView.ItemContainerStyle}">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>

                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
        </Popup>

        <!--#endregion-->

        <!--#region Казина -->

        <Popup IsOpen="{Binding TrashPopup.IsOpen, Mode=TwoWay}"
               PlacementTarget="{Binding ElementName=OpenPasswordTrashManagementButton}" 
               Placement="Custom"
               CustomPopupPlacementCallback="{Binding TrashPopup.CustomPlacementCallback}"
               StaysOpen="False"
               PopupAnimation="Fade"
               AllowsTransparency="True"
               Width="300"
               MaxHeight="{Binding TrashPopup.MaxHeight}">
            <Border Style="{StaticResource Border.Popup}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <UniformGrid Columns="2" HorizontalAlignment="Stretch">
                        <Button Content="Удалить все" 
                                Command="{Binding EmptyTrashCommand}"
                                Style="{StaticResource Button.Delete}"
                                Margin="5"
                                HorizontalAlignment="Stretch"/>

                        <Button Content="Восстановить все" 
                                Command="{Binding RestoreAllTrashCommand}"
                                Style="{StaticResource Button.Restore}" 
                                Margin="5"
                                HorizontalAlignment="Stretch"/>
                    </UniformGrid>

                    <ListView Grid.Row="1"
                              ItemsSource="{Binding TrashPasswords}" 
                              SelectedItem="{Binding SelectedTrashEncryptedOverview}"
                              SelectionMode="Single"
                              Background="Transparent"
                              BorderThickness="0"
                              HorizontalContentAlignment="Stretch"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.CanContentScroll="False">

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid HorizontalAlignment="Stretch" Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Column="0" 
                                           Source="{Binding Icon}" 
                                           Width="27" 
                                           Height="27" 
                                           Margin="5,0,10,0"
                                           VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1" 
                                               Text="{Binding ServiceName}" 
                                               Foreground="White" 
                                               FontSize="18"
                                               VerticalAlignment="Center" 
                                               TextTrimming="CharacterEllipsis"/>

                                    <TextBlock x:Name="ExpireDateTextBlock" Grid.Column="2"
                                               Text="{Binding DeletedAt, Converter={StaticResource DateToDaysRemainingConverter}}"
                                               Foreground="White" 
                                               FontSize="18"
                                               VerticalAlignment="Center"
                                               Visibility="Hidden"/>

                                    <Button Grid.Column="3"
                                            Style="{StaticResource Button.ListView.Menu}"
                                            VerticalAlignment="Center"
                                            Margin="5,0">
                                        <Button.Content>
                                            <Control Template="{StaticResource RestoreCircle}" Width="20" Foreground="Green"/>
                                        </Button.Content>
                                        <b:Interaction.Triggers>
                                            <b:EventTrigger EventName="Click">
                                                <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                                        PropertyName="IsSelected" Value="True" />
                                                <b:InvokeCommandAction Command="{Binding DataContext.RestoreTrashCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                                       CommandParameter="{Binding}"/>
                                            </b:EventTrigger>
                                        </b:Interaction.Triggers>
                                    </Button>
                                </Grid>
                                <DataTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ExpireDateTextBlock" Property="Visibility" Value="Visible" />
                                    </Trigger>
                                </DataTemplate.Triggers>

                            </DataTemplate>
                        </ListView.ItemTemplate>

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" BasedOn="{StaticResource ListView.ItemContainerStyle}">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="BorderThickness" Value="0"/>

                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>

                </Grid>

            </Border>
        </Popup>

        <!--#endregion-->

        <!--#region Контекстное меню у списка с паролям -->

        <!-- Модальный оверлей для PasswordMenuPopup -->
        <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" Grid.RowSpan="3" Panel.ZIndex="2"
                Background="Transparent"
                Visibility="{Binding PasswordMenuPopup.IsOpen, Converter={StaticResource BoolToVisibilityConverter}}" d:Visibility="Collapsed">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDown">
                    <b:InvokeCommandAction Command="{Binding PasswordMenuPopup.HideCommand}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Border>

        <Popup IsOpen="{Binding PasswordMenuPopup.IsOpen}"
               StaysOpen="{Binding PasswordMenuPopup.StaysOpen}"
               PlacementTarget="{Binding PasswordMenuPopup.PlacementTarget}"
               Placement="{Binding PasswordMenuPopup.CurrentPlacement}"
               AllowsTransparency="True"
               PopupAnimation="Slide">

            <Border Background="#282828"
                    BorderBrush="White"
                    BorderThickness="0.5"
                    Padding="5" 
                    CornerRadius="5">

                <DockPanel>
                    <UniformGrid DockPanel.Dock="Top" Columns="3" Rows="0">

                        <Button Grid.Column="0"
                                Command="{Binding MoveToTrashCommand}"
                                CommandParameter="{Binding SelectedEncryptedOverview}"
                                Style="{StaticResource Button.ListView.Menu}" 
                                HorizontalContentAlignment="Center">
                            <Button.Content>
                                <Control Template="{StaticResource Trash}" Width="20" Foreground="White"/>
                            </Button.Content>
                        </Button>

                        <Button Grid.Column="1" Style="{StaticResource Button.ListView.Menu}" HorizontalContentAlignment="Center">
                            <Button.Content>
                                <Control Template="{StaticResource Clipboard}" Width="20" Foreground="White"/>
                            </Button.Content>
                        </Button>

                        <Button Grid.Column="2" Style="{StaticResource Button.ListView.Menu}"
                                Command="{Binding SetArchiveCommand}"
                                CommandParameter="{Binding SelectedEncryptedOverview}"
                                HorizontalContentAlignment="Center">
                            <Button.Content>
                                <Control Template="{StaticResource Archive}" Width="20" Foreground="White"/>
                            </Button.Content>
                        </Button>

                    </UniformGrid>

                    <Button Content="Скопировать" Style="{StaticResource Button.ListView.Menu}" HorizontalContentAlignment="Left"
                            DockPanel.Dock="Top"/>
                    <Button Content="Переместить" Style="{StaticResource Button.ListView.Menu}" HorizontalContentAlignment="Left"
                            DockPanel.Dock="Top"/>
                    <Button Content="Избранное" Style="{StaticResource Button.ListView.Menu}" HorizontalContentAlignment="Left"
                            DockPanel.Dock="Top"/>
                    <Button Content="Добавить Тэг" 
                            Command="{Binding OpenAttachTagPopupCommandCommand}"
                            CommandParameter="{Binding ElementName=PassPage}"
                            Style="{StaticResource Button.ListView.Menu}" 
                            HorizontalContentAlignment="Left"
                            DockPanel.Dock="Top"/>
                    <!--<Button Content="Закрыть"
                            Command="{Binding PasswordMenuPopup.HideCommand}"
                            Style="{StaticResource Button.ListView.Menu}"
                            VerticalAlignment="Bottom"/>-->
                </DockPanel>
            </Border>
        </Popup>

        <!--#endregion-->

        <!--#region Прикрепление тэга к записи -->

        <!-- Модальный оверлей для AttachTagsPopup -->
        <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" Grid.RowSpan="3" Panel.ZIndex="2"
                Background="Transparent"
                Visibility="{Binding AttachTagsPopup.IsOpen, Converter={StaticResource BoolToVisibilityConverter}}" d:Visibility="Collapsed">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDown">
                    <b:InvokeCommandAction Command="{Binding AttachTagsPopup.HideCommand}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Border>

        <Popup IsOpen="{Binding AttachTagsPopup.IsOpen}"
               StaysOpen="{Binding AttachTagsPopup.StaysOpen}"
               PlacementTarget="{Binding AttachTagsPopup.PlacementTarget}" 
               Placement="{Binding AttachTagsPopup.CurrentPlacement}"
               CustomPopupPlacementCallback="{Binding AttachTagsPopup.CustomPlacementCallback}"
               AllowsTransparency="True"
               PopupAnimation="Fade">

            <Border Background="#282828"
                    BorderBrush="White"
                    BorderThickness="0.5"
                    Padding="5" 
                    CornerRadius="5"
                    Width="300" Height="300">
                <ListView Grid.Row="1" 
                          ItemsSource="{Binding Tags}"
                          SelectedItem="{Binding SelectedAttachedTag}" 
                          Style="{StaticResource ListView.App}" 
                         
                          SelectionMode="Single"
                          ScrollViewer.CanContentScroll="False"
                          VirtualizingPanel.ScrollUnit="Pixel"
                          ScrollViewer.VerticalScrollBarVisibility="Visible"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          VirtualizingPanel.CacheLength="15,15"
                          VirtualizingPanel.CacheLengthUnit="Item"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TagName}" 
                                       Foreground="{Binding Color}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       TextWrapping="NoWrap" 
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource ListView.ItemContainerStyle}">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="Margin" Value="0,1,0,0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="attach:ControlHelper.CornerRadius" Value="5"/>

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAttached}" Value="True">
                                    <Setter Property="Background" Value="#005A9E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </DataTrigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1E2F3A"/>
                                    <Setter Property="Foreground" Value="#50A7E7"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#1E2F3A"/>
                                    <Setter Property="Foreground" Value="#50A7E7"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                </ListView>
            </Border>
        </Popup>

        <!--#endregion-->

        <!--=========Controls==========-->

        <!--#region Сортировка, Группировка -->

        <DockPanel Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="1" Margin="5,0,0,0" LastChildFill="False">

            <!--#region Группировка -->

            <Border Style="{StaticResource Border.Base.Card}" Margin="0,0,7,0" DockPanel.Dock="Left">
                <WrapPanel Margin="2">
                    <Button Command="{Binding SetGroupingPasswordsCommand}"
                            CommandParameter="{x:Static enum:GroupingView.None}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0">
                        <Button.ToolTip>
                            <ToolTip Content="Группировка: Отсутствует" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Empty}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding SetGroupingPasswordsCommand}"
                            CommandParameter="{x:Static enum:GroupingView.Name}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0">
                        <Button.ToolTip>
                            <ToolTip Content="Группировка: по имени" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource BookAView}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetGroupingPasswordsCommand}"
                            CommandParameter="{x:Static enum:GroupingView.VaultType}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0">
                        <Button.ToolTip>
                            <ToolTip Content="Группировка: По типу пароля" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource BookTypeView}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetGroupingPasswordsCommand}"
                            CommandParameter="{x:Static enum:GroupingView.Add}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0">
                        <Button.ToolTip>
                            <ToolTip Content="Группировка: По дате добавления" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource ClockPlusView}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetGroupingPasswordsCommand}"
                            CommandParameter="{x:Static enum:GroupingView.Update}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0">
                        <Button.ToolTip>
                            <ToolTip Content="Группировка: По дате обновления" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Updated}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding SetGroupingPasswordsCommand}"
                            CommandParameter="{x:Static enum:GroupingView.Tag}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0">
                        <Button.ToolTip>
                            <ToolTip Content="Группировка: По тэгам" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Tag}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </Border>
            
            <!--#endregion-->

            <!--#region Выбор сортировки -->

            <Border Style="{StaticResource Border.Base.Card}" DockPanel.Dock="Left" Margin="0,0,7,0">
                <WrapPanel Margin="2 0 0 0">
                    <Button Command="{Binding SetSortPasswordsCommand}"
                            CommandParameter="{x:Static enum:SortingView.None}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Left">
                        <Button.ToolTip>
                            <ToolTip Content="Без сортировки" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource ArrowUpDown}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetSortPasswordsCommand}"
                            CommandParameter="{x:Static enum:SortingView.Ascending}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Left">
                        <Button.ToolTip>
                            <ToolTip Content="Краткая детализация" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Ascending}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetSortPasswordsCommand}"
                            CommandParameter="{x:Static enum:SortingView.Descending}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Left">
                        <Button.ToolTip>
                            <ToolTip Content="Компактная детализация" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Descending}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </Border>

            <!--#endregion-->

            <!--#region Выбор темплейта -->
           
            <Border Style="{StaticResource Border.Base.Card}" DockPanel.Dock="Left">
                <WrapPanel Margin="2">
                    <Button Command="{Binding SetTemplatePasswordsCommand}"
                            CommandParameter="{x:Static enum:TemplateType.Detailed}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Left">
                        <Button.ToolTip>
                            <ToolTip Content="Максимальная детализация" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource DetailedView}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetTemplatePasswordsCommand}"
                            CommandParameter="{x:Static enum:TemplateType.Brief}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Left">
                        <Button.ToolTip>
                            <ToolTip Content="Краткая детализация" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource BriefView}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetTemplatePasswordsCommand}"
                            CommandParameter="{x:Static enum:TemplateType.Compact}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Left">
                        <Button.ToolTip>
                            <ToolTip Content="Компактная детализация" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Top}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource GridCompactView}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </Border>

            <Border Style="{StaticResource Border.Base.Card}" DockPanel.Dock="Right" Margin="0, 0, 10, 0">
                <Button Command="{Binding ReloadVaultsCommand}"
                        Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}">
                    <Button.ToolTip>
                        <ToolTip Content="Перезагрузить данные" 
                                 Placement="Custom"
                                 CustomPopupPlacementCallback="{Binding TopToolTipController.CustomPlacementCallback}"
                                 Style="{StaticResource ToolTip.App.Placement.Top}"/>
                    </Button.ToolTip>
                    <StackPanel>
                        <Viewbox>
                            <Control Template="{StaticResource Reload}" Foreground="White"/>
                        </Viewbox>
                    </StackPanel>
                </Button>
            </Border>
            
            <!--#endregion-->

        </DockPanel>

        <!--#endregion-->

        <!--#region Левое боковое меню (Папки, тэги, история записи, архив, карзина) -->

        <Border Grid.Row="0" Grid.RowSpan="2"
                Style="{StaticResource Border.Base.Card}"
                Margin="0,0,0,0"
                CornerRadius="0,10,10,0">

            <Grid Margin="5,5,5,7">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!--=========Контролы=========-->

                <!--#region Переключение списков -->

                <WrapPanel Margin="5,0,0,0" Grid.Row="0">

                    <Button Command="{Binding SetLeftSideMenuControlCommand}"
                            CommandParameter="{x:Static enum:UserControlsName.Tags}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Top">
                        <Button.ToolTip>
                            <ToolTip Content="Тэги" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Tag}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetLeftSideMenuControlCommand}"
                            CommandParameter="{x:Static enum:UserControlsName.Icons}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Top">
                        <Button.ToolTip>
                            <ToolTip Content="Иконки" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource Image}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                </WrapPanel>

                <!--#endregion-->

                <!--#region [UserControl(ListView)] Тэги -->

                <uc:TagsControl x:Name="UcTags" Grid.Row="1"
                                Tags="{Binding Tags}"
                                SelectedTag="{Binding SelectedTag, Mode=TwoWay}"
                                DeleteTagCommand="{Binding DeleteTagCommand}"
                                CreateTagCommand="{Binding CreateTagCommand}"
                                SaveTagCommand="{Binding UpdateTagCommand}"
                                TagName="{Binding NameTag, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Red="{Binding Red, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" UpdateRed="{Binding UpdateRed, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                Green="{Binding Green, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" UpdateGreen="{Binding UpdateGreen, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                Blue="{Binding Blue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" UpdateBlue="{Binding UpdateBlue, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                Visibility="{Binding CurrentDisplayUserControlLeftSideMenu, Converter={StaticResource PasswordPageControlsVisibilityConverter}, ConverterParameter={x:Static enum:UserControlsName.Tags}}"
                                d:Visibility="Collapsed"/>

                <!--#endregion-->

                <!--#region [ListView] Svg Icon -->

                <Grid Grid.Row="1" Visibility="{Binding CurrentDisplayUserControlLeftSideMenu, Converter={StaticResource PasswordPageControlsVisibilityConverter}, ConverterParameter={x:Static enum:UserControlsName.Icons}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <WrapPanel HorizontalAlignment="Right">

                        <Button x:Name="AddIconCatBtn"
                                Command="{Binding AddIconCategoryPopup.ShowCommand}"
                                CommandParameter="{Binding ElementName=AddIconCatBtn}" 
                                Style="{StaticResource Button.App}"
                                Margin="0,0,5,0"
                                Width="30" Height="30">
                            <Button.ToolTip>
                                <ToolTip Content="Добавить категорию" 
                                         Placement="Custom"
                                         CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                         Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                            </Button.ToolTip>
                            <Button.Content>
                                <Control Template="{StaticResource ListFilterPlus}"/>
                            </Button.Content>
                        </Button>
                        <!--CommandParameter="{Binding ElementName=AddIconBtn}"-->
                        <Button x:Name="AddIconBtn"
                                Command="{Binding NavigateToAssetsWebCommand}"
                                Style="{StaticResource Button.App}"
                                Width="30" Height="30">
                            <Button.ToolTip>
                                <ToolTip Content="Добавить иконку" 
                                         Placement="Custom"
                                         CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                         Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                            </Button.ToolTip>
                            <Button.Content>
                                <Control Template="{StaticResource ImagePlus}"/>
                            </Button.Content>
                        </Button>

                    </WrapPanel>

                    <ListView Grid.Row="1" 
                              ItemsSource="{Binding IconView}" d:ItemsSource="{d:SampleData ItemCount=10}"
                              SelectedItem="{Binding SelectedIcon}"
                              ScrollViewer.CanContentScroll="False"
                              VirtualizingPanel.ScrollUnit="Pixel"
                              ScrollViewer.VerticalScrollBarVisibility="Visible"
                              VirtualizingPanel.IsVirtualizing="False"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              VirtualizingPanel.CacheLength="15,15"
                              VirtualizingPanel.CacheLengthUnit="Item">

                        <ListView.Style>
                            <Style TargetType="ListView" BasedOn="{StaticResource ListView.App}">
                                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                                <Setter Property="ItemsPanel" Value="{StaticResource WrapPanelTemplate}"/>
                                <Setter Property="ItemTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <Image Source="{Binding Icon}"
                                                   Width="50" Height="50"
                                                   Stretch="Uniform"
                                                   DockPanel.Dock="Left"
                                                   ToolTip="{Binding Name}"
                                                   ToolTipService.Placement="Right"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.Style>

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" BasedOn="{StaticResource ListView.ItemContainerStyle}">
                                <Setter Property="Margin" Value="3,0,0,0"/>
                                <Setter Property="BorderThickness" Value="0,0,0,5"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderThickness" Value="0,0,0,5"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="BorderThickness" Value="0,0,0,5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListView.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Установить"
                                          Command="{Binding DataContext.UpdateIconSecretCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                          CommandParameter="{Binding DataContext.SelectedIcon, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <MenuItem Header="Удалить">
                                    <MenuItem.Icon>
                                        <Control Template="{StaticResource Trash}" Foreground="Red"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </ListView.ContextMenu>
                        
                        <ListView.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="GroupItem" BasedOn="{StaticResource ListView.GroupItemStyle}">
                                        <Setter Property="Margin" Value="0,5,0,0"/>
                                        <Setter Property="Padding" Value="0,8,0,4"/>
                                        <Setter Property="Background" Value="#10FFFFFF"/>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </ListView.GroupStyle>

                    </ListView>
                </Grid>

                <!--#endregion-->

                <!--#region История, Архив, Карзина -->

                <StackPanel Grid.Row="2">

                    <ToggleButton x:Name="OpenPasswordHistoryManagementButton" 
                                  Content="История" 
                                  IsChecked="{Binding IsPasswordToDeletePopupOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  attach:ButtonHelper.IconTemplate="{StaticResource Updated}"
                                  Foreground="White">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton" BasedOn="{StaticResource ToggleButton.Page.SideMenu.Management}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ArchivedSecrets.Count}" Value="0">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>

                    <ToggleButton x:Name="OpenArchiveManagementButton" 
                                  Content="Архив" 
                                  Command="{Binding ArchivesPopup.ShowCommand}"
                                  CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                                  IsChecked="{Binding ArchivesPopup.IsOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  attach:ButtonHelper.IconTemplate="{StaticResource Archive}"
                                  VerticalAlignment="Bottom" Margin="5,0,0,0">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton" BasedOn="{StaticResource ToggleButton.Page.SideMenu.Management}">
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="Gray"/>
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>

                    <ToggleButton x:Name="OpenPasswordTrashManagementButton" 
                                  Content="Корзина" 
                                  Command="{Binding TrashPopup.ShowCommand}"
                                  CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                                  IsChecked="{Binding TrashPopup.IsOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  attach:ButtonHelper.IconTemplate="{StaticResource Trash}"
                                  VerticalAlignment="Bottom" Margin="5,0,0,0">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton" BasedOn="{StaticResource ToggleButton.Page.SideMenu.Management}">
                                <!--<Setter Property="Foreground" Value="#FF3333"/>-->
                                <Setter Property="Foreground" Value="#D70D0D"/>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Foreground" Value="#AE0606"/>
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>

                </StackPanel>

                <!--#endregion-->

            </Grid>
        </Border>

        <!--#endregion-->

        <GridSplitter Grid.Column="1" Grid.RowSpan="2"
                      Width="3" 
                      ResizeDirection="Columns" 
                      ResizeBehavior="BasedOnAlignment"
                      Margin="2,0,2,0"
                      HorizontalAlignment="Center"/>

        <!--#region Список паролей -->

        <ListView x:Name="PasswordsListView" Grid.Column="2" Grid.Row="1"
                  ItemsSource="{Binding PasswordsView}" d:ItemsSource="{d:SampleData ItemCount=5}"
                  SelectedItem="{Binding SelectedEncryptedOverview}"
                  SelectionMode="Single"
                  ScrollViewer.CanContentScroll="False"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.CacheLength="15,15"
                  VirtualizingPanel.CacheLengthUnit="Item">

            <ListView.Resources>
                
                <!--#region Подробно описание элемента -->

                <DataTemplate x:Key="DetailedSecrets">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!--#region Иконка сервиса -->

                        <StackPanel Grid.RowSpan="2"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Margin="5,0,10,0">

                            <Image Source="{Binding Icon}"
                                   Stretch="Uniform"/>

                        </StackPanel>

                        <!--#endregion-->

                        <Grid Grid.Column="1" Margin="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!--#region Название записи -->

                            <TextBlock Text="{Binding ServiceName}" d:Text="YouTube"
                                       DockPanel.Dock="Top"
                                       VerticalAlignment="Center"
                                       FontFamily="Leelawadee"
                                       FontSize="18"
                                       FontWeight="Bold"/>

                            <!--#endregion-->

                            <!--#region Элементы управления: Меню, избранное, список тэгов -->

                            <WrapPanel Grid.Column="1" HorizontalAlignment="Right">

                                <Border Background="Transparent"
                                        VerticalAlignment="Center"
                                        ToolTipService.InitialShowDelay="100"
                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListView}}">

                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Tags.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Tags}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <Border.ToolTip>
                                        <ToolTip Placement="Custom"
                                                 CustomPopupPlacementCallback="{Binding PlacementTarget.Tag.BottomToolTipController.CustomPlacementCallback, RelativeSource={RelativeSource Self}}">
                                            <ToolTip.Template>
                                                <ControlTemplate TargetType="ToolTip">
                                                    <Grid DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ToolTip}}">

                                                        <Path HorizontalAlignment="Center" 
                                                              VerticalAlignment="Top"
                                                              Fill="#2D2F31"
                                                              Stroke="#555759"
                                                      Data="M 5,0 L 0,8 L 10,8 Z" />

                                                        <Border Background="#2D2F31" 
                                                                BorderBrush="#555759" 
                                                                BorderThickness="1" 
                                                                CornerRadius="5"
                                                                Padding="10"
                                                                Margin="0,8,0,0">

                                                            <ItemsControl ItemsSource="{Binding Tags}">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <TextBlock Text="{Binding TagName}" 
                                                                                   Foreground="White"
                                                                                   FontSize="14" 
                                                                                   Margin="0,2"/>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </Border>
                                                    </Grid>
                                                </ControlTemplate>
                                            </ToolTip.Template>
                                        </ToolTip>
                                    </Border.ToolTip>

                                    <Control Template="{StaticResource ScrollText}"
                                             Width="24" 
                                             Height="24"
                                             IsHitTestVisible="False"/>
                                </Border>

                                <Button Command="{Binding DataContext.SetFavoriteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding }"
                                        Margin="2,0,0,0">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                                    PropertyName="IsSelected"
                                                                    Value="True" />
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource Button.App}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Viewbox>
                                                        <Control Template="{StaticResource AddFavorite}" Width="20" Foreground="#35A500"/>
                                                    </Viewbox>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Viewbox>
                                                                <Control Template="{StaticResource RemoveFavorite}" Width="20" Foreground="Yellow"/>
                                                            </Viewbox>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button x:Name="PopupButton"
                                        Command="{Binding DataContext.PasswordMenuPopup.ShowCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding ElementName=PopupButton}"
                                        Style="{StaticResource Button.App}"
                                        Margin="2,0,0,0">
                                    <Button.Content>
                                        <Viewbox>
                                            <Control Template="{StaticResource DotsMenu}" Width="25" Foreground="White"/>
                                        </Viewbox>
                                    </Button.Content>
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                                    PropertyName="IsSelected"
                                                                    Value="True" />
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Button>

                            </WrapPanel>

                            <!--#endregion-->

                        </Grid>

                        <!--#region Кратная информация о сервисе, кнопки управление -->

                        <DockPanel Grid.Column="1" Grid.Row="1" Margin="3">

                            <!--#region Добавлен -->

                            <StackPanel DockPanel.Dock="Left" Width="200">
                                <WrapPanel>
                                    <Viewbox Width="15" Margin="0,0,5,0">
                                        <Control Template="{StaticResource FilledCorrect}"/>
                                    </Viewbox>
                                    <TextBlock Text="Добавлен" DockPanel.Dock="Top" Foreground="#438DB0"/>
                                </WrapPanel>

                                <TextBlock Text="{Binding DateAdded, Converter={StaticResource RecordDateUpdateConverter}}" d:Text="17 Июня 2026 11:11"/>
                            </StackPanel>

                            <!--#endregion-->

                            <!--#region Изменен -->

                            <StackPanel Margin="20,0,0,0" DockPanel.Dock="Left" Width="200">
                                <WrapPanel>
                                    <Viewbox Width="15" Margin="0,0,5,0">
                                        <Control Template="{StaticResource Updated}" Foreground="#10A64A"/>
                                    </Viewbox>
                                    <TextBlock Text="Обновлен" DockPanel.Dock="Top" Foreground="#438DB0"/>
                                </WrapPanel>

                                <TextBlock Text="{Binding DateUpdate, Converter={StaticResource RecordDateUpdateConverter}}" d:Text="17 Июня 2026 11:11"/>
                            </StackPanel>

                            <!--#endregion-->

                            <!--#region Гипперссылка -->

                            <StackPanel Margin="20,0,0,0" DockPanel.Dock="Left">
                                <WrapPanel>
                                    <Viewbox Width="15" Margin="0,0,5,0">
                                        <Control Template="{StaticResource Source}" Foreground="#10A64A"/>
                                    </Viewbox>
                                    <TextBlock Text="Источник" DockPanel.Dock="Top" Foreground="#438DB0"/>
                                </WrapPanel>

                                <TextBlock>
                            <Hyperlink NavigateUri="{Binding Url}">
                                <Hyperlink.Style>
                                    <Style TargetType="Hyperlink">
                                       <Setter Property="Foreground" Value="#009EFF" /> 
                                       <Setter Property="TextDecorations" Value="Underline" />
                                       <Style.Triggers>
                                           <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                                               <Setter Property="Foreground" Value="#50A7E7"/>
                                           </DataTrigger>
                                       </Style.Triggers>
                                   </Style>
                               </Hyperlink.Style>
                               <TextBlock Text="Перейти" 
                                          TextTrimming="CharacterEllipsis"
                                          TextWrapping="NoWrap"/>
                               <b:Interaction.Triggers>
                                   <b:EventTrigger EventName="RequestNavigate">
                                       <behaviors:InvokeCommandWithHandled Command="{Binding DataContext.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding Url}" />
                                   </b:EventTrigger>
                               </b:Interaction.Triggers>
                           </Hyperlink>
                        </TextBlock>
                            </StackPanel>

                            <!--#endregion-->

                        </DockPanel>

                        <!--#endregion-->

                    </Grid>
                </DataTemplate>

                <!--#endregion-->

                <!--#region Краткое описание элемент -->

                <DataTemplate x:Key="ShortlySecrets">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!--#region Иконка сервиса -->

                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Margin="5,0,10,0">

                            <Image Source="{Binding Icon}"
                                   Height="40"
                                   Stretch="Uniform"/>

                        </StackPanel>

                        <!--#endregion-->

                        <!--#region Контент -->

                        <DockPanel Grid.Column="1">

                            <!--#region Название записи -->
   
                            <TextBlock Text="{Binding ServiceName}" d:Text="YouTube"
                                       FontFamily="Leelawadee"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"
                                       DockPanel.Dock="Left"
                                       VerticalAlignment="Center"/>
                            
                            <!--#endregion-->

                            <!--#region Элементы управления: Меню, избранное, список тэгов -->
                           
                            <WrapPanel Grid.Column="1" HorizontalAlignment="Right">

                                <Border Background="Transparent"
                                        VerticalAlignment="Center"
                                        ToolTipService.InitialShowDelay="100"
                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListView}}">

                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Tags.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Tags}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <Border.ToolTip>
                                        <ToolTip Placement="Custom"
                                                 CustomPopupPlacementCallback="{Binding PlacementTarget.Tag.BottomToolTipController.CustomPlacementCallback, RelativeSource={RelativeSource Self}}">
                                            <ToolTip.Template>
                                                <ControlTemplate TargetType="ToolTip">
                                                    <Grid DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ToolTip}}">

                                                        <Path HorizontalAlignment="Center" 
                                                              VerticalAlignment="Top"
                                                              Fill="#2D2F31"
                                                              Stroke="#555759"
                                                              Data="M 5,0 L 0,8 L 10,8 Z" />

                                                        <Border Background="#2D2F31" 
                                                                BorderBrush="#555759" 
                                                                BorderThickness="1" 
                                                                CornerRadius="5"
                                                                Padding="10"
                                                                Margin="0,8,0,0">

                                                            <ItemsControl ItemsSource="{Binding Tags}">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <TextBlock Text="{Binding TagName}" 
                                                                                   Foreground="White"
                                                                                   FontSize="14" 
                                                                                   Margin="0,2"/>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </Border>
                                                    </Grid>
                                                </ControlTemplate>
                                            </ToolTip.Template>
                                        </ToolTip>
                                    </Border.ToolTip>

                                    <Control Template="{StaticResource ScrollText}"
                                             Width="24" 
                                             Height="24"
                                             IsHitTestVisible="False"/>
                                </Border>

                                <Button Command="{Binding DataContext.SetFavoriteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding }"
                                        Margin="2,0,0,0">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                                    PropertyName="IsSelected"
                                                                    Value="True" />
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource Button.App}">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <Viewbox>
                                                        <Control Template="{StaticResource AddFavorite}" Width="20" Foreground="#35A500"/>
                                                    </Viewbox>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Viewbox>
                                                                <Control Template="{StaticResource RemoveFavorite}" Width="20" Foreground="Yellow"/>
                                                            </Viewbox>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button x:Name="PopupButton"
                                        Command="{Binding DataContext.PasswordMenuPopup.ShowCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding ElementName=PopupButton}"
                                        Style="{StaticResource Button.App}"
                                        Margin="2,0,0,0">
                                    <Button.Content>
                                        <Viewbox>
                                            <Control Template="{StaticResource DotsMenu}" Width="25" Foreground="White"/>
                                        </Viewbox>
                                    </Button.Content>
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:ChangePropertyAction TargetObject="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                                                    PropertyName="IsSelected"
                                                                    Value="True" />
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Button>
                            </WrapPanel>
                            
                            <!--#endregion-->

                        </DockPanel>

                        <!--#endregion-->

                    </Grid>
                </DataTemplate>

                <!--#endregion-->

                <!--#region Очень краткое описание элемента -->

                <DataTemplate x:Key="VeryBrieflySecret">
                    <Image Source="{Binding Icon}"
                           Width="80" Height="80"
                           Stretch="Uniform"
                           DockPanel.Dock="Left"
                           ToolTip="{Binding ServiceName}"/>
                </DataTemplate>

                <!--#endregion-->
                
            </ListView.Resources>
            
            <ListView.Style>
                <Style TargetType="ListView" BasedOn="{StaticResource ListView.App}">
                    <Setter Property="ItemTemplate" Value="{StaticResource DetailedSecrets}"/>
                    <Setter Property="ItemsPanel" Value="{StaticResource StackPanelTemplate}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentTemplateTypePasswords}" Value="{x:Static enum:TemplateType.Detailed}">
                            <Setter Property="ItemTemplate" Value="{StaticResource DetailedSecrets}"/>
                            <Setter Property="ItemsPanel" Value="{StaticResource StackPanelTemplate}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentTemplateTypePasswords}" Value="{x:Static enum:TemplateType.Brief}">
                            <Setter Property="ItemTemplate" Value="{StaticResource ShortlySecrets}"/>
                            <Setter Property="ItemsPanel" Value="{StaticResource StackPanelTemplate}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentTemplateTypePasswords}" Value="{x:Static enum:TemplateType.Compact}">
                            <Setter Property="ItemTemplate" Value="{StaticResource VeryBrieflySecret}"/>
                            <Setter Property="ItemsPanel" Value="{StaticResource WrapPanelTemplate}"/>
                            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource ListView.ItemContainerStyle}">
                    <Setter Property="attach:ControlHelper.RightClickCommand" Value="{Binding DataContext.SelectAndShowPasswordMenuPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DataContext.CurrentTemplateTypePasswords, RelativeSource={RelativeSource AncestorType=ListView}}" Value="{x:Static enum:TemplateType.Compact}">
                            <Setter Property="BorderThickness" Value="0,0,0,5"/>
                            <Setter Property="Margin" Value="3,0,0,0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="GroupItem" BasedOn="{StaticResource ListView.GroupItemStyle}">
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Padding" Value="0,0,0,5"/>
                            <Setter Property="Background" Value="{StaticResource App.SurfaceBrush}"/>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ListView.GroupStyle>

        </ListView>

        <!--#endregion-->

        <!--#region Правая боковая панель -->

        <Border Grid.Column="3" Grid.RowSpan="2"
                Style="{StaticResource Border.Base.Card}"
                Visibility="{Binding IsSidebarOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                Width="{Binding RightSidebarWidth}" 
                CornerRadius="10,0,0,10">
            <Border.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Margin" Value="5,0,0,0"/>
                </Style>

                <Style TargetType="TextBox" BasedOn="{StaticResource TextBox.InputData}">
                    <!--<Setter Property="Template" Value="{StaticResource InputPasswordDataTemplate}"/>-->
                </Style>

            </Border.Resources>
            <Grid Margin="5,5,5,7">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!--#region Переключение действий -->

                <WrapPanel Margin="5,0,0,0" Grid.Row="0" HorizontalAlignment="Right">

                    <Button Command="{Binding SetRightSideMenuActionCommand}"
                            CommandParameter="{x:Static enum:ActionOnData.View}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Top">
                        <Button.ToolTip>
                            <ToolTip Content="Просмотр" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource LookingEye}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetRightSideMenuActionCommand}"
                            CommandParameter="{x:Static enum:ActionOnData.Create}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Top">
                        <Button.ToolTip>
                            <ToolTip Content="Создать" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource CirclePlus}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding SetRightSideMenuActionCommand}"
                            CommandParameter="{x:Static enum:ActionOnData.Update}"
                            Style="{StaticResource Buttom.PasswordPage.SideMenu.Navigation}"
                            Margin="3,0,0,0"
                            DockPanel.Dock="Top">
                        <Button.ToolTip>
                            <ToolTip Content="Редактировать" 
                                     Placement="Custom"
                                     CustomPopupPlacementCallback="{Binding BottomToolTipController.CustomPlacementCallback}"
                                     Style="{StaticResource ToolTip.App.Placement.Bottom}"/>
                        </Button.ToolTip>
                        <StackPanel>
                            <Viewbox>
                                <Control Template="{StaticResource SquarePen}" Foreground="White"/>
                            </Viewbox>
                        </StackPanel>
                    </Button>

                </WrapPanel>

                <!--#endregion-->

                <ScrollViewer Grid.Row="1">

                    <Grid Margin="10,0,0,0">
                        <DockPanel>
                            <ComboBox ItemsSource="{Binding PasswordTypes}" 
                                      SelectedItem="{Binding SelectedPasswordType}" 
                                      Style="{StaticResource BaseComboBox}"
                                      IsEnabled="{Binding CurrentActionRightSideMenu, Converter={StaticResource ActionOnDataToIsReadOnlyConverter}}"
                                      DockPanel.Dock="Top">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Value}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                            </ComboBox>

                            <ContentControl Content="{Binding SelectedCredentialItemBaseViewModel}"
                                            HorizontalAlignment="Stretch">
                                <d:ContentControl.Content>

                                    <pvm:StandardPasswordViewModel ServiceName="Тестовый Сервис"
                                                                   Login="admin_user"
                                                                   Url="https://example.com"
                                                                   Note="Это пример заметки для отображения в дизайнере."/>
                                </d:ContentControl.Content>
                            </ContentControl>

                        </DockPanel>

                    </Grid>


                </ScrollViewer>

            </Grid>

        </Border>

        <!--#endregion-->

    </Grid>
</Page>
